export default function IdeaCanvas() {
  return (
    <div className="max-w-2xl">
      <div className="text-xs text-muted mb-1">Working title</div>
      <h1 className="text-2xl font-semibold mb-4">
        Sidekick — context-aware build workspace
      </h1>
      <p className="text-sm text-muted leading-relaxed">
        One workspace where an early-stage team moves an idea from brief to
        research to deck to shipped UI, with an agent that can see every tab
        instead of asking you to paste things twice.
      </p>
    </div>
  );
}
